public class Basic_2D {

	public static void main(String[] args) {

		/*
		 * basic two d programme to uderstand how 2d array works in this program we
		 * create new 2d array index print element to this 2d array two d array always
		 * work two foor loop(main and nested)
		 */

		int two[][] = new int[5][5];
		int count = 1;

		for (int i = 0; i < 5; i++) {
			for (int j = 0; j < 5; j++) {
				two[i][j] = count;
				System.out.print(two[i][j] + " ");
				count++;

			}
			System.out.println();
			count = 1;
		}
	}
}
